This directory contains native C bindings used by `lua-lib` modules, independent
from the main C runtime. Build from the repository root:

```sh
cmake --preset c-bindings
cmake --build --preset c-bindings
```
